name: 合并广告规则列表

on:
  schedule:
    # 每天凌晨2点自动运行
    - cron: '0 2 * * *'
  workflow_dispatch:  # 允许手动触发

permissions:
  contents: write

jobs:
  merge-rules:
    runs-on: ubuntu-latest
    steps:
      - name: 检出仓库代码
        uses: actions/checkout@v3

      - name: 下载并合并规则
        run: |
          # 创建临时目录
          mkdir -p temp_rules
          
          # 规则列表（你提供的7个）
          RULES=(
            "https://adguardteam.github.io/HostlistsRegistry/assets/filter_21.txt"
            "https://raw.githubusercontent.com/xinggsf/Adblock-Plus-Rule/master/rule.txt"
            "https://adguardteam.github.io/HostlistsRegistry/assets/filter_1.txt"
            "https://raw.githubusercontent.com/TG-Twilight/AWAvenue-Ads-Rule/main/AWAvenue-Ads-Rule.txt"
            "https://raw.githubusercontent.com/TG-Twilight/AWAvenue-Ads-Rule/main/Filters/AWAvenue-Ads-Rule-Replenish.txt"
            "https://adguardteam.github.io/HostlistsRegistry/assets/filter_53.txt"
            "https://adguardteam.github.io/HostlistsRegistry/assets/filter_29.txt"
          )
          
          # 下载所有规则文件
          for i in "${!RULES[@]}"; do
            echo "下载规则 $((i+1)): ${RULES[$i]}"
            curl -L -f -o "temp_rules/rule_$i.txt" "${RULES[$i]}" || echo "⚠️ 警告: 下载失败 ${RULES[$i]}"
          done
          
          # 创建合并文件
          > merged_rules.txt
          
          # 处理每个规则文件 - 精准提取广告拦截规则
          for file in temp_rules/*.txt; do
            if [ -f "$file" ]; then
              echo "处理文件: $file"
              
              # 使用更精确的 grep 模式来匹配 AdBlock Plus 规则
              grep -E '^\|\|[^\^]+\^|^[.[:alnum:]]' "$file" | \
                # 排除以感叹号 (!) 开头的注释行
                grep -v '^!' | \
                # 排除以 # 开头的注释行
                grep -v '^#' | \
                # 排除以 @@ 开头的例外规则
                grep -v '^@@' | \
                # 排除包含 domain= 的行（通常是规则修饰符）
                grep -v 'domain=' >> merged_rules.txt
            fi
          done
          
          # 排序去重
          sort -u merged_rules.txt -o merged_rules.txt
          
          # 统计行数
          RULE_COUNT=$(wc -l < merged_rules.txt)
          echo "📊 合并后规则总数: $RULE_COUNT"
          
          # 显示前10条预览
          echo "📝 规则预览（前10条）:"
          head -10 merged_rules.txt

      - name: 检查文件是否有变化
        id: check_changes
        run: |
          if [ -f merged_rules.txt ] && [ -f adblock_rules.txt ]; then
            if diff -q merged_rules.txt adblock_rules.txt >/dev/null; then
              echo "✅ 规则无变化，跳过提交"
              echo "changed=false" >> $GITHUB_OUTPUT
              rm -f merged_rules.txt
            else
              echo "🔄 规则有变化，准备更新"
              echo "changed=true" >> $GITHUB_OUTPUT
              mv merged_rules.txt adblock_rules.txt
            fi
          else
            echo "🔄 首次运行，创建规则文件"
            echo "changed=true" >> $GITHUB_OUTPUT
            mv merged_rules.txt adblock_rules.txt
          fi

      - name: 提交更新文件
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add adblock_rules.txt
          git commit -m "📦 自动合并广告规则 - $(date +'%Y-%m-%d') (共 $(wc -l < adblock_rules.txt) 条)"
          git push

      - name: 生成规则统计信息
        if: steps.check_changes.outputs.changed == 'true'
        run: |
          {
            echo "# 广告规则合并统计"
            echo ""
            echo "## 基本信息"
            echo "- 更新时间: $(date +'%Y-%m-%d %H:%M:%S')"
            echo "- 总规则数: $(wc -l < adblock_rules.txt)"
            echo "- 规则来源: 7个广告拦截列表"
            echo ""
            echo "## 规则预览（前20条）"
            echo '```'
            head -20 adblock_rules.txt
            echo '```'
            echo ""
            echo "## 使用说明"
            echo "在 AdGuard Home 中添加订阅链接:"
            echo '```'
            echo "https://raw.githubusercontent.com/${{ github.repository }}/main/adblock_rules.txt"
            echo '```'
          } > stats.md

      - name: 上传统计信息
        if: steps.check_changes.outputs.changed == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: rule-stats
          path: stats.md
